# Public environment variables for the frontend (Next.js)
# Next.js automatically loads .env.local and .env files
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser

# Backend API base URL
# Example for local dev backend:
NEXT_PUBLIC_API_URL=http://localhost:8000

# Expected Cardano network for the connected wallet
# Accepted values: mainnet | preprod | preview | testnet (maps to test network id 0)
NEXT_PUBLIC_CARDANO_NETWORK=preprod
